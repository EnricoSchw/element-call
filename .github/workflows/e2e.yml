name: Run E2E tests
on: push
jobs:
  e2e:
    name: Build E2E tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out test private repo
        uses: actions/checkout@v3
        with:
          repository: vector-im/static-call-participant
          ref: refs/heads/e2e
          path: static-call-participant
          token: ${{ secrets.CHECKOUT_PRIVATE_REPO }}
      - name: Run tests
        run: "docker run --rm -v '${{ github.workspace }}/static-call-participant :/opt/' node:18 ./opt/e2e.sh"
